<%= render partial: "application/breadcrumb", 
locals: {list: @breadcrumbs} %>
<%= render partial: "application/record_limiter", 
locals: {collection: @subject.posts, class_name: "posts-container"} %>

<%= render 'posts/form', subject: @subject %>