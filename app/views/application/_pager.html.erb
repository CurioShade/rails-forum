<nav>
    <% if @page != 1 %>
        <a href="<%= url_clean_path %>"><<</a>
        <a href="<%= url_clean_path + "?page=#{@page-1}"%>"><</a>
    <% end %>
    <p><%= @page.to_s + " of #{max_pages}" %></p>
    <% if @page != max_pages %>
        <a href="<%= url_clean_path + "?page=#{@page+1}" %>">></a>
        <a href="<%= url_clean_path + "?page=#{max_pages}" %>">>></a>
    <% end %>
</nav>